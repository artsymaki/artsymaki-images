# artsymaki-images

Relay for generated blog images. Public only so WordPress can fetch them by URL.
Filenames carry a timestamp and are never reused, because raw.githubusercontent.com caches for ~5 minutes.
